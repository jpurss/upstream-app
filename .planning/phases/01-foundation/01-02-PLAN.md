---
description: "Plan 02 for Phase 1 Foundation — auth Server Actions (signup, login, logout, demo bypass), login page UI with two-column layout and demo CTAs, signup page, and auth callback route."
date_last_edited: 2026-03-25
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - app/(auth)/login/page.tsx
  - app/(auth)/signup/page.tsx
  - app/(auth)/login/actions.ts
  - app/(auth)/signup/actions.ts
  - app/auth/callback/route.ts
  - lib/auth-utils.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - AUTH-05
  - UI-02

must_haves:
  truths:
    - "User can sign up with email and password and lands on /library"
    - "User can log in with email and password and lands on /library"
    - "User can click 'Explore as Consultant' on login page and enter app as anonymous consultant"
    - "User can click 'Explore as Admin' on login page and enter app as anonymous admin"
    - "Login page shows product context with feature highlights on the left and auth panel on the right"
    - "Demo buttons are hero-level prominence above the email/password form"
    - "Session persists across browser refresh"
  artifacts:
    - path: "app/(auth)/login/page.tsx"
      provides: "Two-column login page with demo CTAs, feature highlights, and login form"
      min_lines: 80
    - path: "app/(auth)/login/actions.ts"
      provides: "Server Actions: signIn, signInAsDemo"
      exports: ["signIn", "signInAsDemo"]
    - path: "app/(auth)/signup/page.tsx"
      provides: "Signup page with email/password form"
      min_lines: 40
    - path: "app/(auth)/signup/actions.ts"
      provides: "Server Action: signUp"
      exports: ["signUp"]
    - path: "app/auth/callback/route.ts"
      provides: "Supabase auth callback handler for email confirmation"
      exports: ["GET"]
  key_links:
    - from: "app/(auth)/login/page.tsx"
      to: "app/(auth)/login/actions.ts"
      via: "form action binding"
      pattern: "signIn|signInAsDemo"
    - from: "app/(auth)/login/actions.ts"
      to: "lib/supabase/server.ts"
      via: "import createClient"
      pattern: "import.*createClient.*from.*@/lib/supabase/server"
    - from: "app/(auth)/login/actions.ts"
      to: "/library"
      via: "redirect after successful auth"
      pattern: "redirect.*library"
---

<objective>
Build the complete authentication flow: Server Actions for signup, login, logout, and demo bypass, plus the login page UI matching the UI-SPEC two-column layout with hero demo CTAs.

Purpose: Users need to be able to enter the application — either by signing up/logging in with email or by clicking the demo bypass buttons. The login page is the first thing the target user (Brendan) sees, so it must communicate product value immediately.

Output: Working auth flow — signup, login, demo bypass as consultant, demo bypass as admin — all redirecting to /library.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-UI-SPEC.md
@.planning/phases/01-foundation/01-01-SUMMARY.md (Supabase client patterns from Plan 01)

<interfaces>
<!-- From Plan 01 — executor needs these contracts -->

From lib/supabase/server.ts:
```typescript
export async function createClient(): Promise<SupabaseClient>
```

From lib/supabase/client.ts:
```typescript
export function createClient(): SupabaseClient
```

From components/ui/button.tsx (shadcn):
```typescript
// Standard shadcn Button with variant, size, asChild props
```

From components/ui/input.tsx (shadcn):
```typescript
// Standard shadcn Input component
```

From components/ui/label.tsx (shadcn):
```typescript
// Standard shadcn Label component
```

From components/ui/separator.tsx (shadcn):
```typescript
// Standard shadcn Separator component
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Auth Server Actions and callback route</name>
  <files>
    app/(auth)/login/actions.ts
    app/(auth)/signup/actions.ts
    app/auth/callback/route.ts
    lib/auth-utils.ts
    tests/auth-actions.test.ts
  </files>
  <read_first>
    lib/supabase/server.ts (createClient pattern from Plan 01)
    .planning/phases/01-foundation/01-RESEARCH.md (Pattern 4: Anonymous Sign-In, Pattern 3: Auth Hook, Anti-Patterns section)
    .planning/phases/01-foundation/01-CONTEXT.md (D-01, D-04, D-07, D-13)
  </read_first>
  <behavior>
    - Test 1: signIn action calls supabase.auth.signInWithPassword with provided email and password
    - Test 2: signUp action calls supabase.auth.signUp with provided email and password
    - Test 3: signInAsDemo('consultant') calls supabase.auth.signInAnonymously with data.demo_role='consultant' and data.display_name='Demo Consultant'
    - Test 4: signInAsDemo('admin') calls supabase.auth.signInAnonymously with data.demo_role='admin' and data.display_name='Demo Admin'
    - Test 5: signOut action calls supabase.auth.signOut and redirects to /login
    - Test 6: All successful auth actions redirect to /library (per D-07)
    - Test 7: signIn returns error message on invalid credentials
  </behavior>
  <action>
    1. Create `app/(auth)/login/actions.ts` with two Server Actions:

       `signIn(formData: FormData)`:
       - Extract email and password from formData
       - Call `supabase.auth.signInWithPassword({ email, password })`
       - On error: return `{ error: 'Incorrect email or password. Please try again.' }` (per UI-SPEC Copywriting Contract)
       - On success: `redirect('/library')` (per D-07)

       `signInAsDemo(role: 'consultant' | 'admin')`:
       - Call `supabase.auth.signInAnonymously()` with options:
         ```typescript
         {
           options: {
             data: {
               demo_role: role,
               display_name: role === 'admin' ? 'Demo Admin' : 'Demo Consultant',
             }
           }
         }
         ```
       - Per D-13: uses `signInAnonymously()` for per-session isolation
       - Per D-03: display_name stored in user_metadata for UI display only (NOT for RBAC)
       - On error: return `{ error: 'Something went wrong. Please try again.' }` (per UI-SPEC Copywriting Contract)
       - On success: `redirect('/library')`

    2. Create `app/(auth)/signup/actions.ts` with one Server Action:

       `signUp(formData: FormData)`:
       - Extract email and password from formData
       - Call `supabase.auth.signUp({ email, password })`
       - On error: return `{ error: error.message }`
       - On success: redirect to `/login` with a success message (or to /library if auto-confirmed)

    3. Create `app/auth/callback/route.ts`:
       ```typescript
       import { NextResponse } from 'next/server'
       import { createClient } from '@/lib/supabase/server'

       export async function GET(request: Request) {
         const { searchParams, origin } = new URL(request.url)
         const code = searchParams.get('code')
         const next = searchParams.get('next') ?? '/library'

         if (code) {
           const supabase = await createClient()
           const { error } = await supabase.auth.exchangeCodeForSession(code)
           if (!error) {
             return NextResponse.redirect(`${origin}${next}`)
           }
         }
         return NextResponse.redirect(`${origin}/login?error=auth`)
       }
       ```

    4. Create `lib/auth-utils.ts` with shared utilities:
       ```typescript
       export function getSignOutAction() {
         // Server Action for signOut — used by sidebar logout button
       }
       ```

       `signOut` Server Action:
       - Call `supabase.auth.signOut()`
       - `redirect('/login')` (per AUTH-03)

    All Server Actions must use `'use server'` directive at top of file.
    All Server Actions must use `createClient` from `@/lib/supabase/server` (NOT the browser client).
    IMPORTANT: Use `getUser()` for any server-side auth checks, NEVER `getSession()` (per RESEARCH anti-patterns).
  </action>
  <verify>
    <automated>npx vitest run tests/auth-actions.test.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `app/(auth)/login/actions.ts` contains `'use server'` directive
    - `app/(auth)/login/actions.ts` contains `export async function signIn(formData: FormData)`
    - `app/(auth)/login/actions.ts` contains `export async function signInAsDemo(role: 'consultant' | 'admin')`
    - `app/(auth)/login/actions.ts` contains `signInAnonymously` and `demo_role` and `display_name`
    - `app/(auth)/login/actions.ts` contains `redirect('/library')`
    - `app/(auth)/signup/actions.ts` contains `'use server'` and `export async function signUp`
    - `app/auth/callback/route.ts` contains `export async function GET` and `exchangeCodeForSession`
    - `lib/auth-utils.ts` contains `signOut` and `redirect('/login')`
    - No file imports from `@/lib/supabase/client` (all server-side)
    - No file uses `getSession()` (anti-pattern)
    - `tests/auth-actions.test.ts` exists and tests pass
  </acceptance_criteria>
  <done>Auth Server Actions work: signIn with email/password, signUp with email/password, signInAsDemo with consultant or admin role, signOut clears session and redirects to /login. Auth callback handles email confirmation code exchange.</done>
</task>

<task type="auto">
  <name>Task 2: Login page and signup page UI</name>
  <files>
    app/(auth)/login/page.tsx
    app/(auth)/signup/page.tsx
    app/(auth)/layout.tsx
  </files>
  <read_first>
    .planning/phases/01-foundation/01-UI-SPEC.md (Login Page section, Copywriting Contract, Interaction States, Color section, Typography section)
    .planning/phases/01-foundation/01-CONTEXT.md (D-01, D-02, D-04)
    app/(auth)/login/actions.ts (the actions this page calls — from Task 1)
    components/ui/button.tsx (shadcn Button component API)
    components/ui/input.tsx (shadcn Input component API)
    components/ui/separator.tsx (shadcn Separator component API)
  </read_first>
  <action>
    1. Create `app/(auth)/layout.tsx`:
       - Simple centered layout for auth pages
       - Full viewport height, zinc-950 background
       - No sidebar, no demo banner (these appear post-login only)

    2. Create `app/(auth)/login/page.tsx` — Two-column login page (per UI-SPEC Login Page section):

       **Left column (60% width, min 1280px for two-column):**
       - Product wordmark "Upstream" (20px semibold, Geist Sans, white text)
       - Tagline: "GitHub for Prompts" or "Every engagement makes your firm smarter" (14px zinc-400)
       - 3 feature highlights with Lucide icons:
         a. `Library` icon + "Prompt Library" heading (16px semibold) + "Central collection of field-tested prompts, searchable by category, model, and effectiveness." (14px zinc-400)
         b. `GitFork` icon + "Fork & Adapt" heading + "Check out prompts into client engagements. Adapt, test, and rate in the field." (14px zinc-400)
         c. `GitMerge` icon + "Merge & Improve" heading + "Push winning improvements back. Every engagement makes the firm smarter." (14px zinc-400)
       - Left column vertically centered, with generous padding (3xl = 64px)

       **Right column (40% width) — Auth panel:**
       Per UI-SPEC, vertical order top to bottom:
       1. "Upstream" wordmark — 20px semibold, Geist Sans, white
       2. 32px vertical gap
       3. "EXPLORE THE DEMO" section label — 13px semibold, zinc-400, uppercase, tracking-wide
       4. "Explore as Consultant" button — full-width, accent fill (#4287FF bg, white text), 16px semibold. onClick calls `signInAsDemo('consultant')` (per D-04)
       5. "Explore as Admin" button — full-width, accent outline (border #4287FF, text #4287FF), 16px semibold. onClick calls `signInAsDemo('admin')` (per D-04)
       6. "No signup required" subtext — 13px, zinc-500, centered (per UI-SPEC Copywriting)
       7. Separator with "or" label — shadcn Separator, zinc-800 border, "or" in 13px zinc-500 (positioned over the line)
       8. Email input with label "Email" — 14px, zinc-900 bg, zinc-800 border (per UI-SPEC)
       9. Password input with label "Password" — same styling
       10. "Sign In" button — full-width, accent fill, 14px semibold (per UI-SPEC)
       11. "Don't have an account? Sign up" — 13px zinc-500, "Sign up" as link in #4287FF pointing to /signup (per UI-SPEC Copywriting)

       **Error handling:**
       - Display error messages below the form in red-500 text
       - Use `useActionState` (React 19) or `useFormState` for Server Action error feedback
       - Error copy per UI-SPEC: "Incorrect email or password. Please try again." or "Something went wrong. Please try again."

       **Loading states:**
       - Demo buttons and Sign In button show spinner and disable during submission (per UI-SPEC Interaction States: loading state = spinner, disabled)

       **Component usage:**
       - Use `"use client"` since form needs interactivity
       - Import `signIn`, `signInAsDemo` from `./actions`
       - Use shadcn `Button`, `Input`, `Label`, `Separator` components
       - Use Lucide icons: `Library`, `GitFork`, `GitMerge`

       **Responsive behavior (per D-18, desktop-only):**
       - Below 1280px: stack to single column (auth panel only, hide feature highlights)
       - This is a safety fallback, not a design priority

    3. Create `app/(auth)/signup/page.tsx`:
       - Similar auth panel style to login (single column centered, max-width 400px)
       - "Upstream" wordmark at top
       - Email input, Password input, "Sign Up" button (accent fill)
       - "Already have an account? Sign in" link to /login
       - Uses `signUp` from `./actions`
       - Error/loading handling same pattern as login
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `app/(auth)/login/page.tsx` contains `"use client"` directive
    - `app/(auth)/login/page.tsx` contains `Explore as Consultant` and `Explore as Admin` (per D-04 copy)
    - `app/(auth)/login/page.tsx` contains `signInAsDemo` import from `./actions`
    - `app/(auth)/login/page.tsx` contains `No signup required` (per UI-SPEC Copywriting)
    - `app/(auth)/login/page.tsx` contains shadcn component imports: `Button`, `Input`, `Separator`
    - `app/(auth)/login/page.tsx` contains Lucide icon imports for feature highlights
    - `app/(auth)/login/page.tsx` contains `#4287FF` or uses shadcn `primary` variant for accent buttons
    - `app/(auth)/signup/page.tsx` contains `signUp` import and email/password form
    - `app/(auth)/layout.tsx` exists
    - `npm run build` succeeds without errors
  </acceptance_criteria>
  <done>Login page renders with two-column layout: feature highlights on left, demo CTAs (hero prominence) + login form on right. Signup page renders with centered form. Both pages connect to Server Actions for auth flow. All copy matches UI-SPEC Copywriting Contract.</done>
</task>

</tasks>

<verification>
- `npm run build` succeeds
- Login page renders at /login with two-column layout
- "Explore as Consultant" button triggers anonymous sign-in with consultant demo_role
- "Explore as Admin" button triggers anonymous sign-in with admin demo_role
- Email/password form submits via signIn Server Action
- Signup page at /signup creates new account
- Auth callback route exchanges code for session
- signOut action clears session and redirects to /login
</verification>

<success_criteria>
- Complete auth flow working: signup, login, demo bypass (both roles), logout
- Login page matches UI-SPEC layout — demo CTAs are hero prominence, feature highlights visible
- All copy matches Copywriting Contract exactly
- Sessions persist across refresh (via proxy.ts from Plan 01)
- Error states display correct messages
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
