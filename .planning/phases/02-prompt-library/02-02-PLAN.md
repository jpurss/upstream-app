---
description: "Plan 02 for Phase 02 — Library browse page with prompt card grid/list view, filter bar with search/sort/dropdowns, filter chips, empty state, and loading skeleton. Covers LIB-04, LIB-05, LIB-06."
date_last_edited: 2026-03-25
phase: 02-prompt-library
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - app/(app)/library/page.tsx
  - app/(app)/library/loading.tsx
  - components/library/library-grid.tsx
  - components/library/prompt-card.tsx
  - components/library/prompt-card-list.tsx
  - components/library/filter-bar.tsx
  - components/library/filter-chips.tsx
autonomous: true
requirements: [LIB-04, LIB-05, LIB-06]
must_haves:
  truths:
    - "User can see prompt cards in a responsive grid layout showing title, category badge, model badge, description, capability type, star rating, and checkout count"
    - "User can toggle between grid and list views using icon buttons"
    - "User can filter prompts by category, capability type, industry, effectiveness range, status, and target model — results update instantly"
    - "User can search prompts by keyword with debounced input — matching prompts appear"
    - "User can sort prompts by highest rated, most used, newest, or alphabetical"
    - "Active filters appear as dismissible chips with a clear-all button"
    - "Result count shows 'N of M prompts' format"
    - "When no prompts match filters, an empty state message with clear-all button appears"
    - "Filter and sort state is synced to URL params for bookmarkable/shareable URLs"
  artifacts:
    - path: "app/(app)/library/page.tsx"
      provides: "Server component that fetches initial prompts and passes to client grid"
      contains: "fetchAllActivePrompts"
    - path: "app/(app)/library/loading.tsx"
      provides: "Loading skeleton for library page"
      contains: "Skeleton"
    - path: "components/library/library-grid.tsx"
      provides: "Client component managing filter state, search, sort, and rendering grid/list"
      contains: "'use client'"
    - path: "components/library/prompt-card.tsx"
      provides: "Individual prompt card component for grid view"
      contains: "Card"
    - path: "components/library/prompt-card-list.tsx"
      provides: "Individual prompt row component for list view"
      contains: "Link"
    - path: "components/library/filter-bar.tsx"
      provides: "Filter controls: search input, select dropdowns, slider, sort, view toggle"
      contains: "Select"
    - path: "components/library/filter-chips.tsx"
      provides: "Active filter chips with dismiss and clear-all"
      contains: "Badge"
  key_links:
    - from: "app/(app)/library/page.tsx"
      to: "lib/data/prompts.ts"
      via: "fetchAllActivePrompts import"
      pattern: "import.*fetchAllActivePrompts.*from.*@/lib/data/prompts"
    - from: "components/library/library-grid.tsx"
      to: "lib/supabase/client.ts"
      via: "browser client for search re-queries"
      pattern: "import.*createClient.*from.*@/lib/supabase/client"
    - from: "components/library/library-grid.tsx"
      to: "nuqs"
      via: "useQueryState for URL filter state"
      pattern: "import.*useQueryState.*from.*nuqs"
    - from: "components/library/prompt-card.tsx"
      to: "app/(app)/library/[promptId]/page.tsx"
      via: "Link to detail page"
      pattern: "href=.*library/"
---

<objective>
Build the complete library browse page — the primary surface where users discover and explore prompts. This includes the responsive card grid with toggle to list view, the full filter bar with search/sort/dropdowns, active filter chips, empty states, and loading skeletons.

Purpose: This is the most-used page in the app. Users need to quickly scan available prompts, filter to their needs, and click through to details. The filter/search UX must feel instant and the URL state must be shareable.

Output: Fully functional `/library` page with 7 component files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-prompt-library/02-CONTEXT.md
@.planning/phases/02-prompt-library/02-RESEARCH.md
@.planning/phases/02-prompt-library/02-UI-SPEC.md
@.planning/phases/02-prompt-library/02-01-SUMMARY.md

<interfaces>
<!-- From Plan 01: lib/types/prompt.ts -->
```typescript
export type PromptCategory = 'Discovery' | 'Solution Design' | 'Build' | 'Enablement' | 'Delivery' | 'Internal Ops'
export type PromptCapabilityType = 'extraction' | 'analysis' | 'generation' | 'transformation' | 'evaluation' | 'synthesis'
export type PromptStatus = 'active' | 'deprecated'
export type PromptComplexity = 'basic' | 'moderate' | 'advanced'
export type Prompt = {
  id: string; title: string; description: string | null; content: string;
  version: number; category: PromptCategory; capability_type: PromptCapabilityType;
  industry_tags: string[]; use_case_tags: string[]; target_model: string;
  complexity: PromptComplexity; status: PromptStatus;
  avg_effectiveness: number; total_checkouts: number; total_ratings: number;
  created_at: string; updated_at: string;
  /* ... other fields */
}
```

<!-- From Plan 01: lib/data/prompts.ts -->
```typescript
export async function fetchAllActivePrompts(): Promise<Prompt[]>
export async function fetchPromptById(id: string): Promise<Prompt | null>
export async function searchPrompts(query: string): Promise<Prompt[]>
```

<!-- shadcn base API — Select requires items prop, Slider uses scalar defaultValue -->
<!-- Project uses base: "base" (not radix) — see .claude/skills/shadcn/rules/base-vs-radix.md -->

<!-- Existing app/(app)/layout.tsx passes effectiveRole via server-side role check -->
<!-- NuqsAdapter already wrapped around children (from Plan 01) -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build prompt card components (grid + list) and library-grid client shell</name>
  <files>components/library/prompt-card.tsx, components/library/prompt-card-list.tsx, components/library/library-grid.tsx</files>
  <read_first>
    - lib/types/prompt.ts (Prompt type — all fields available for display)
    - components/ui/card.tsx (Card component API — CardHeader, CardTitle, CardContent, CardFooter)
    - components/ui/badge.tsx (Badge component API — variant prop)
    - .planning/phases/02-prompt-library/02-UI-SPEC.md (card specs, colors, typography, spacing)
    - .planning/phases/02-prompt-library/02-CONTEXT.md (D-01 through D-03 card decisions)
    - .claude/skills/shadcn/rules/styling.md (cn(), no space-y, semantic colors)
    - .claude/skills/shadcn/rules/icons.md (data-icon attribute)
    - .claude/skills/shadcn/rules/composition.md (Card full composition, Badge for status)
  </read_first>
  <behavior>
    - Test 1: PromptCard renders title, description (truncated), category badge, model badge, star rating with count, and checkout count
    - Test 2: PromptCard wraps in a Link pointing to /library/{id}
    - Test 3: PromptCardList renders title, category, capability type, model, rating, and checkouts in a row layout
    - Test 4: LibraryGrid renders prompt cards in grid mode by default
    - Test 5: LibraryGrid renders prompt rows when viewMode is 'list'
    - Test 6: LibraryGrid shows empty state message when filtered prompts array is empty
  </behavior>
  <action>
    **`components/library/prompt-card.tsx`** — `'use client'` component:
    - Props: `{ prompt: Prompt }`
    - Wraps entire card in `<Link href={/library/${prompt.id}}>` for D-03 (whole card clickable)
    - Uses shadcn `Card`, `CardHeader`, `CardContent` composition
    - Card surface: uses default Card styles (already `--card` background, `--border` border from shadcn)
    - Hover: `cn("transition-colors hover:border-primary/30")` per UI-SPEC border glow, no shadow
    - Layout inside card:
      - Top row: category `<Badge>` left, model `<Badge variant="secondary">` right — flex justify-between
      - Title: `<CardTitle>` at heading size (16px/600) — `text-base font-semibold`
      - Description: `<p className="text-sm text-muted-foreground line-clamp-1">` per UI-SPEC (body 14px, truncated)
      - Tags row: capability_type badge + first industry tag — `text-[13px] text-muted-foreground`
      - Bottom row: Star rating (lucide `Star` icon filled in `text-[#FFB852]` for warning color) + `avg_effectiveness.toFixed(1)` + rating count in muted, lucide `Download` icon + `total_checkouts` in muted — flex justify-between
    - Star rendering: Show the numeric value + star icon, not 5 individual stars (for density per UI-SPEC)
    - Card padding: `p-4` (md = 16px per spacing scale)
    - Card gap between grid items: handled by parent grid

    **`components/library/prompt-card-list.tsx`** — `'use client'` component:
    - Props: `{ prompt: Prompt }`
    - Wraps in `<Link href={/library/${prompt.id}}>` — whole row clickable
    - Single row, 48px height per UI-SPEC list row height
    - Columns: title (flex-1), category badge, capability_type, target_model badge, rating (star + number), checkouts (icon + number)
    - Hover: `hover:bg-card/50` subtle highlight
    - Border bottom: `border-b border-border`
    - All text at body/label sizes per typography spec

    **`components/library/library-grid.tsx`** — `'use client'` top-level client component:
    - Props: `{ initialPrompts: Prompt[], isAdmin: boolean, totalCount: number }`
    - Uses nuqs `useQueryState` with `parseAsString` for: `q` (search), `category`, `capability`, `industry`, `model`, `status`, `sort`, `view`
    - Uses nuqs `parseAsFloat` for: `rating` (min effectiveness)
    - Manages `prompts` state initialized from `initialPrompts` prop (see RESEARCH.md Pitfall 5 — use a key or derive from prop)
    - Actually, to avoid Pitfall 5: use `initialPrompts` directly in `useMemo` for client-side filtering, and only set separate state for search results from Supabase re-queries
    - Search debounce: `useEffect` with 300ms `setTimeout` + cleanup. When search query present, calls Supabase browser client `searchPrompts`. When empty, reverts to `initialPrompts`.
    - Client-side filtering in `useMemo`: filter by category, capability_type, industry (check `industry_tags.includes()`), target_model, status, min effectiveness (`avg_effectiveness >= rating`). Sort by: `highest-rated` (avg_effectiveness desc), `most-used` (total_checkouts desc), `newest` (created_at desc), `a-z` (title asc)
    - Renders: FilterBar, FilterChips, then either grid of PromptCards or list of PromptCardList rows based on `view` state
    - Grid layout: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4`
    - Empty state: When filtered.length === 0, render centered empty state with lucide `SearchX` icon (40px, muted), heading "No prompts match your filters", body text, and "Clear all filters" ghost button (per UI-SPEC empty state and copywriting contract)
    - Result count: `"{filtered.length} of {totalCount} prompts"` displayed in label size muted text
    - The `isAdmin` prop is passed through for Plan 04 to add a "New Prompt" button conditionally — for now just accept the prop
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - components/library/prompt-card.tsx contains "'use client'"
    - components/library/prompt-card.tsx contains "import Link from 'next/link'"
    - components/library/prompt-card.tsx contains "/library/${prompt.id}" or template literal equivalent
    - components/library/prompt-card.tsx contains "hover:border-primary/30"
    - components/library/prompt-card.tsx contains "line-clamp-1"
    - components/library/prompt-card.tsx contains "Star" (lucide icon import)
    - components/library/prompt-card.tsx contains "Download" (lucide icon import)
    - components/library/prompt-card-list.tsx contains "'use client'"
    - components/library/prompt-card-list.tsx contains "import Link from 'next/link'"
    - components/library/library-grid.tsx contains "'use client'"
    - components/library/library-grid.tsx contains "import { useQueryState" or "useQueryState"
    - components/library/library-grid.tsx contains "parseAsString"
    - components/library/library-grid.tsx contains "No prompts match your filters"
    - components/library/library-grid.tsx contains "SearchX" (lucide icon for empty state)
    - npm test exits with code 0
  </acceptance_criteria>
  <done>Prompt card components render all metadata fields per D-02. Library-grid client shell manages filter state via nuqs, applies client-side filtering/sorting, renders grid or list view, and shows empty state. All tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build filter bar, filter chips, and search with debounce</name>
  <files>components/library/filter-bar.tsx, components/library/filter-chips.tsx</files>
  <read_first>
    - components/ui/select.tsx (Select component — base API requires items prop)
    - components/ui/slider.tsx (Slider component — base API uses scalar defaultValue)
    - components/ui/input.tsx (Input component)
    - components/ui/badge.tsx (Badge for filter chips)
    - components/ui/button.tsx (Button for clear all, view toggle)
    - components/ui/tooltip.tsx (Tooltip for view toggle buttons)
    - .planning/phases/02-prompt-library/02-UI-SPEC.md (filter bar interaction patterns, spacing, colors)
    - .planning/phases/02-prompt-library/02-CONTEXT.md (D-04 through D-07, D-17)
    - .claude/skills/shadcn/rules/base-vs-radix.md (Select items prop pattern, Slider scalar value)
    - .claude/skills/shadcn/rules/forms.md (FieldGroup/Field not needed for filter bar — these are standalone controls)
    - .claude/skills/shadcn/rules/icons.md (data-icon on icons in buttons)
    - supabase/seed.sql (first 70 lines — to extract actual category/model values for dropdown options)
  </read_first>
  <behavior>
    - Test 1: FilterBar renders a search input with placeholder "Search prompts..."
    - Test 2: FilterBar renders Select dropdowns for category, capability type, industry, target model, status
    - Test 3: FilterBar renders a Slider for effectiveness range
    - Test 4: FilterBar renders sort dropdown with 4 options: "Highest rated", "Most used", "Newest first", "A–Z"
    - Test 5: FilterBar renders grid/list view toggle buttons
    - Test 6: FilterChips renders a chip for each active filter with a dismiss button
    - Test 7: FilterChips renders "Clear all" button when any filter is active
    - Test 8: FilterChips shows result count in format "N of M prompts"
  </behavior>
  <action>
    **`components/library/filter-bar.tsx`** — `'use client'` component:
    - Props: All nuqs setter functions and current values passed from library-grid parent:
      ```typescript
      type FilterBarProps = {
        search: string; onSearchChange: (val: string) => void
        category: string; onCategoryChange: (val: string | null) => void
        capability: string; onCapabilityChange: (val: string | null) => void
        industry: string; onIndustryChange: (val: string | null) => void
        model: string; onModelChange: (val: string | null) => void
        status: string; onStatusChange: (val: string | null) => void
        minRating: number; onMinRatingChange: (val: number) => void
        sort: string; onSortChange: (val: string) => void
        view: string; onViewChange: (val: string) => void
      }
      ```
    - Layout: Sticky below page header, `bg-background border-b border-border`, 40px control height, `p-4` padding
    - Left section: `<Input>` search field with `placeholder="Search prompts..."`, lucide `Search` icon as visual indicator
    - Center section: 5 `<Select>` dropdowns in a flex row with `gap-2`:
      - Category: items = `[{label: "All categories", value: null}, {label: "Discovery", value: "Discovery"}, ...]` — all 6 PromptCategory values
      - Capability type: items = `[{label: "All types", value: null}, {label: "Extraction", value: "extraction"}, ...]` — all 6 PromptCapabilityType values
      - Industry: items = `[{label: "All industries", value: null}, {label: "Cross-industry", value: "cross-industry"}, {label: "Technology", value: "technology"}, {label: "SaaS", value: "saas"}, {label: "Financial Services", value: "financial-services"}, {label: "Healthcare", value: "healthcare"}, {label: "Retail", value: "retail"}, {label: "Manufacturing", value: "manufacturing"}, {label: "Energy", value: "energy"}]`
      - Target model: items = `[{label: "All models", value: null}, {label: "Claude Sonnet 4", value: "Claude Sonnet 4"}, {label: "GPT-4o", value: "GPT-4o"}, {label: "Gemini 2.5 Pro", value: "Gemini 2.5 Pro"}, {label: "Model-agnostic", value: "model-agnostic"}]`
      - Status: items = `[{label: "All statuses", value: null}, {label: "Active", value: "active"}, {label: "Deprecated", value: "deprecated"}]`
    - CRITICAL: Use the `base` Select API — pass `items` prop on `<Select>`, use `<SelectValue>` without `placeholder` prop, use `<SelectContent alignItemWithTrigger={false} side="bottom">`, wrap items in `<SelectGroup>`.
    - Right section: Effectiveness `<Slider>` (min 0, max 5, step 0.5, `defaultValue={0}` — base API uses scalar not array), sort `<Select>` with items for D-17 sort options, and two view toggle `<Button variant="ghost" size="icon">` — lucide `LayoutGrid` and `List` icons
    - View toggle: Wrap each in `<Tooltip>` with tooltipContent "Grid view" / "List view" per copywriting contract. Active state: `variant="secondary"` when selected, `variant="ghost"` when not
    - IMPORTANT: Use `<Slider defaultValue={minRating} onValueChange={onMinRatingChange} />` — base API slider uses scalar, NOT array
    - All controls are compact — dropdowns at `h-9` (36px min touch target per UI-SPEC)
    - Responsive: On smaller screens, wrap into multiple rows using `flex flex-wrap gap-2`

    **`components/library/filter-chips.tsx`** — `'use client'` component:
    - Props: All current filter values, setter functions (to clear individual), totalCount, filteredCount
    - Renders a row below the filter bar when any filter is active
    - Each active filter = a chip: `<Badge>` with `className="bg-primary/10 text-primary border-primary/20"` (per UI-SPEC accent chip styling), dismiss `X` button via lucide `X` icon
    - Chip label format: "Category: Discovery", "Model: GPT-4o", "Rating: ≥3.5", etc.
    - "Clear all" ghost button: `<Button variant="ghost" className="text-muted-foreground">Clear all</Button>` — calls all setter reset functions
    - Result count: `<span className="text-[13px] text-muted-foreground">{filteredCount} of {totalCount} prompts</span>` — right-aligned
    - Only renders when at least one filter is active (category, capability, industry, model, status not empty, or minRating > 0, or search not empty)
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - components/library/filter-bar.tsx contains "'use client'"
    - components/library/filter-bar.tsx contains "Search prompts..." (placeholder text)
    - components/library/filter-bar.tsx contains "Select" (shadcn Select import)
    - components/library/filter-bar.tsx contains "Slider" (shadcn Slider import)
    - components/library/filter-bar.tsx contains "LayoutGrid" (lucide icon for grid toggle)
    - components/library/filter-bar.tsx contains "List" (lucide icon for list toggle)
    - components/library/filter-bar.tsx contains "Highest rated" (sort option)
    - components/library/filter-bar.tsx contains "items=" or "items={" (base Select API)
    - components/library/filter-chips.tsx contains "'use client'"
    - components/library/filter-chips.tsx contains "bg-primary/10 text-primary"
    - components/library/filter-chips.tsx contains "Clear all"
    - components/library/filter-chips.tsx contains "of" and "prompts" (result count format)
    - npm test exits with code 0
  </acceptance_criteria>
  <done>Filter bar renders all 6 filter dimensions (D-06) plus search, sort (D-17), and view toggle (D-01). Filter chips show active filters as dismissible badges with clear-all and result count (D-07). All controls use shadcn base API correctly. All tests pass.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Wire library page server component and loading skeleton</name>
  <files>app/(app)/library/page.tsx, app/(app)/library/loading.tsx</files>
  <read_first>
    - app/(app)/library/page.tsx (current placeholder page to replace)
    - app/(app)/layout.tsx (understand how role/user data flows)
    - lib/data/prompts.ts (fetchAllActivePrompts function)
    - lib/supabase/server.ts (server client for role check)
    - components/library/library-grid.tsx (client component to render)
    - components/ui/skeleton.tsx (Skeleton component API)
    - .planning/phases/02-prompt-library/02-UI-SPEC.md (loading skeleton specs, page header)
  </read_first>
  <action>
    **Replace `app/(app)/library/page.tsx`** — Server component:
    - Import `fetchAllActivePrompts` from `@/lib/data/prompts`
    - Import `createClient` from `@/lib/supabase/server` for role check
    - Import `LibraryGrid` from `@/components/library/library-grid`
    - Fetch user and determine `effectiveRole` using the same pattern as `app/(app)/layout.tsx`:
      ```typescript
      const supabase = await createClient()
      const { data: { user } } = await supabase.auth.getUser()
      const role = user?.app_metadata?.role as string | null
      const isAnonymous = user?.is_anonymous ?? false
      const demoRole = isAnonymous ? (user?.user_metadata?.demo_role as string ?? 'consultant') : null
      const effectiveRole = role ?? (isAnonymous ? demoRole : null)
      const isAdmin = effectiveRole === 'admin'
      ```
    - Fetch prompts: `const prompts = await fetchAllActivePrompts()`
    - Page header: `<div className="flex items-center justify-between mb-8">` containing:
      - Left: `<h1 className="text-xl font-semibold">Prompt Library</h1>` (display size 20px/600 per typography spec)
      - Right: reserved slot for admin "New Prompt" button (Plan 04 will add `{isAdmin && <NewPromptButton />}`)
    - Pass props: `<LibraryGrid initialPrompts={prompts} isAdmin={isAdmin} totalCount={prompts.length} />`
    - Page wrapper: `<div className="p-8">` (3xl = 64px outer padding — use p-8 = 32px as reasonable with sidebar offset, or use container class)

    **Create `app/(app)/library/loading.tsx`** — Loading skeleton:
    - Import `Skeleton` from `@/components/ui/skeleton`
    - Render page header skeleton + 12 card skeletons in a 3-column grid
    - Card skeleton dimensions: full width, 180px height per UI-SPEC
    - Grid: `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4`
    - Header skeleton: one `<Skeleton className="h-7 w-48" />` for title
    - Each card skeleton: `<Skeleton className="h-[180px] w-full rounded-[0.625rem]" />`
    - Filter bar: no skeleton per UI-SPEC — renders immediately
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20 && echo "Build check complete"</automated>
  </verify>
  <acceptance_criteria>
    - app/(app)/library/page.tsx contains "fetchAllActivePrompts"
    - app/(app)/library/page.tsx contains "LibraryGrid"
    - app/(app)/library/page.tsx contains "isAdmin"
    - app/(app)/library/page.tsx contains "Prompt Library" (page title)
    - app/(app)/library/page.tsx contains "effectiveRole"
    - app/(app)/library/page.tsx is a server component (no 'use client' directive)
    - app/(app)/library/loading.tsx contains "Skeleton"
    - app/(app)/library/loading.tsx contains "grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3"
    - npm run build completes without errors
  </acceptance_criteria>
  <done>Library page fetches prompts server-side and renders LibraryGrid client component with filter/search/sort. Loading skeleton shows 12 card placeholders. Page works for both admin and consultant roles. Build succeeds.</done>
</task>

</tasks>

<verification>
- `npm test` passes
- `npm run build` succeeds
- Library page at `/library` renders prompt cards from seed data
- Filter dropdowns render with correct options
- Search input has correct placeholder
- View toggle switches between grid and list
- Empty state appears when all prompts are filtered out
- URL updates when filters change (e.g., `?category=Discovery&sort=most-used`)
</verification>

<success_criteria>
User can browse 18 seed prompts in a card grid, toggle to list view, filter by 6 dimensions, search by keyword, sort by 4 options, see active filter chips with result count, and share filtered URLs. Empty state displays when no matches found.
</success_criteria>

<output>
After completion, create `.planning/phases/02-prompt-library/02-02-SUMMARY.md`
</output>
