---
description: Wave 0 plan for Phase 04 — creates 6 behavioral test stub files required by VALIDATION.md before any production code is written.
date_last_edited: 2026-03-26
phase: 04-merge-workflow
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/merge-suggest.test.ts
  - tests/merge-data.test.ts
  - tests/review-queue.test.ts
  - tests/merge-approve.test.ts
  - tests/merge-decline.test.ts
  - tests/merge-diff.test.tsx
autonomous: true
requirements:
  - MERGE-01
  - MERGE-02
  - MERGE-03
  - MERGE-04
  - MERGE-05

must_haves:
  truths:
    - "All 6 test files exist and can be discovered by Vitest"
    - "Each test file contains describe blocks with failing (skipped) stubs for the behaviors it covers"
    - "Running npm test succeeds (skipped tests do not fail the suite)"
  artifacts:
    - path: "tests/merge-suggest.test.ts"
      provides: "Behavioral stubs for MERGE-01 — suggestMerge server action"
      min_lines: 20
    - path: "tests/merge-data.test.ts"
      provides: "Behavioral stubs for MERGE-03 — data layer admin client verification"
      min_lines: 20
    - path: "tests/review-queue.test.ts"
      provides: "Behavioral stubs for MERGE-03 — admin gate redirect"
      min_lines: 15
    - path: "tests/merge-approve.test.ts"
      provides: "Behavioral stubs for MERGE-04 — approve action, version bump, changelog"
      min_lines: 25
    - path: "tests/merge-decline.test.ts"
      provides: "Behavioral stubs for MERGE-05 — decline reason storage"
      min_lines: 15
    - path: "tests/merge-diff.test.tsx"
      provides: "Behavioral stubs for MERGE-02 — DiffViewer custom title props"
      min_lines: 15
  key_links: []
---

<objective>
Create the 6 behavioral test stub files documented in VALIDATION.md as Wave 0 requirements. Each file contains `describe` blocks with `it.todo()` or `it.skip()` stubs that define the expected behaviors for this phase's code. These stubs will be filled in (RED step) and then made green by the corresponding production tasks in plans 01-04.

Purpose: CLAUDE.md mandates TDD by default. Wave 0 establishes the test scaffold before any production code is written, ensuring every code-producing task has a test file to run against.

Output: 6 test files in `tests/` directory, all discoverable by Vitest, all passing (skipped stubs do not fail).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-merge-workflow/04-CONTEXT.md
@.planning/phases/04-merge-workflow/04-RESEARCH.md
@.planning/phases/04-merge-workflow/04-VALIDATION.md

<interfaces>
<!-- Existing test mock pattern from tests/library-deprecate.test.tsx -->

Mock pattern for server actions:
```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest'

// Mock Next.js modules
vi.mock('next/cache', () => ({
  revalidatePath: vi.fn(),
}))
vi.mock('next/navigation', () => ({
  redirect: vi.fn(),
  useRouter: vi.fn().mockReturnValue({ push: vi.fn() }),
}))

// Mock sonner
vi.mock('sonner', () => ({
  toast: { success: vi.fn(), error: vi.fn() },
}))

// Mock Supabase server client
const mockEq = vi.fn()
const mockUpdate = vi.fn()
const mockFrom = vi.fn()
const mockGetUser = vi.fn()

vi.mock('@/lib/supabase/server', () => ({
  createClient: vi.fn(() =>
    Promise.resolve({
      auth: { getUser: mockGetUser },
      from: mockFrom,
    })
  ),
}))
```

Mock pattern for admin client:
```typescript
vi.mock('@/lib/supabase/admin', () => ({
  createAdminClient: vi.fn(() => ({
    from: mockAdminFrom,
  })),
}))
```

Component render test pattern:
```typescript
import { render, screen } from '@testing-library/react'
import React from 'react'
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create test stubs for server actions (merge-suggest, merge-approve, merge-decline)</name>
  <files>
    tests/merge-suggest.test.ts,
    tests/merge-approve.test.ts,
    tests/merge-decline.test.ts
  </files>
  <read_first>
    tests/library-deprecate.test.tsx,
    .planning/phases/04-merge-workflow/04-VALIDATION.md,
    .planning/phases/04-merge-workflow/04-RESEARCH.md
  </read_first>
  <action>
    Create 3 test files following the exact mock pattern from `tests/library-deprecate.test.tsx`.
    Each file should set up mocks at the top, then contain `describe` blocks with `it.todo()` stubs
    that name the expected behaviors. The stubs will be filled in by Wave 1 tasks.

    **1. `tests/merge-suggest.test.ts` (MERGE-01):**

    Mocks: `@/lib/supabase/server` (regular client — consultant uses own client).

    ```typescript
    describe('suggestMerge Server Action', () => {
      it.todo('sets merge_status to pending and merge_suggestion to the provided note')
      it.todo('clears merge_decline_reason on resubmission')
      it.todo('returns error when user is not authenticated')
      it.todo('calls revalidatePath for /engagements and /review')
    })
    ```

    **2. `tests/merge-approve.test.ts` (MERGE-04):**

    Mocks: `@/lib/supabase/server` (for auth check), `@/lib/supabase/admin` (for admin client mutations).

    ```typescript
    describe('approveMerge Server Action', () => {
      it.todo('fetches current prompt version before updating')
      it.todo('updates prompt content with approvedContent parameter')
      it.todo('increments prompt version by 1')
      it.todo('creates a changelog entry with previous content and merge note')
      it.todo('sets fork merge_status to approved')
      it.todo('returns error when user is not admin')
      it.todo('returns error when prompt is not found')
      it.todo('calls revalidatePath for /review, /library, and /engagements')
    })
    ```

    **3. `tests/merge-decline.test.ts` (MERGE-05):**

    Mocks: `@/lib/supabase/server` (for auth check), `@/lib/supabase/admin` (for admin client mutations).

    ```typescript
    describe('declineMerge Server Action', () => {
      it.todo('sets merge_status to declined and stores decline reason')
      it.todo('returns error when user is not admin')
      it.todo('calls revalidatePath for /review and /engagements')
    })
    ```

    Each file must have proper vitest imports at top (`import { describe, it, expect, vi, beforeEach } from 'vitest'`) and the mock setup blocks. The `it.todo()` calls define what to test without implementation — they show as "todo" in vitest output and do not fail the suite.
  </action>
  <verify>
    <automated>cd /Users/jpurss/_projects/Upstream && npm test -- tests/merge-suggest.test.ts tests/merge-approve.test.ts tests/merge-decline.test.ts 2>&1 | tail -20</automated>
  </verify>
  <done>3 server action test files exist with todo stubs. `npm test` runs them without failures.</done>
</task>

<task type="auto">
  <name>Task 2: Create test stubs for data layer, review queue, and diff viewer (merge-data, review-queue, merge-diff)</name>
  <files>
    tests/merge-data.test.ts,
    tests/review-queue.test.ts,
    tests/merge-diff.test.tsx
  </files>
  <read_first>
    tests/library-deprecate.test.tsx,
    tests/library-filter.test.tsx,
    .planning/phases/04-merge-workflow/04-VALIDATION.md,
    .planning/phases/04-merge-workflow/04-RESEARCH.md
  </read_first>
  <action>
    Create 3 test files following established mock patterns.

    **1. `tests/merge-data.test.ts` (MERGE-03 — data layer):**

    Mocks: `@/lib/supabase/admin` (admin client used by all data access functions).

    ```typescript
    describe('Merge Data Access Layer', () => {
      describe('fetchMergeSuggestions', () => {
        it.todo('uses createAdminClient, not createClient')
        it.todo('filters by merge_status when status param is not all')
        it.todo('returns all non-none statuses when status param is all')
        it.todo('flattens joined fields into MergeSuggestion shape')
      })

      describe('fetchMergeSuggestionById', () => {
        it.todo('returns null when suggestion not found')
        it.todo('excludes forks with merge_status of none')
      })

      describe('countPendingMerges', () => {
        it.todo('returns count of forks with merge_status pending')
        it.todo('returns 0 when no pending forks exist')
      })
    })
    ```

    **2. `tests/review-queue.test.ts` (MERGE-03 — admin gate):**

    Mocks: `@/lib/supabase/server`, `next/navigation` (for redirect).

    ```typescript
    describe('Review Queue Page — Admin Gate', () => {
      it.todo('redirects non-admin users to /engagements')
      it.todo('redirects unauthenticated users to /login')
      it.todo('allows admin users to view the page')
      it.todo('allows anonymous demo admin to view the page')
    })
    ```

    **3. `tests/merge-diff.test.tsx` (MERGE-02 — DiffViewer props):**

    Mocks: `react-diff-viewer-continued` (mock the component to avoid heavy rendering).

    ```typescript
    import { render, screen } from '@testing-library/react'
    import React from 'react'

    describe('DiffViewer — Custom Title Props', () => {
      it.todo('renders with default titles Original and Adapted when no props given')
      it.todo('renders with custom leftTitle and rightTitle when provided')
      it.todo('shows No differences found when original equals adapted')
    })
    ```

    Each file must have proper vitest imports and mock setup blocks. Use `it.todo()` for all test cases.
  </action>
  <verify>
    <automated>cd /Users/jpurss/_projects/Upstream && npm test -- tests/merge-data.test.ts tests/review-queue.test.ts tests/merge-diff.test.tsx 2>&1 | tail -20</automated>
  </verify>
  <done>3 additional test files exist with todo stubs. `npm test` runs all 6 phase 04 test files without failures.</done>
</task>

</tasks>

<verification>
- `npm test` passes with all 6 new test files discovered
- Each test file contains `describe` and `it.todo()` stubs
- No test failures (todos are skipped, not failures)
- Test file count: merge-suggest, merge-data, review-queue, merge-approve, merge-decline, merge-diff
</verification>

<success_criteria>
All 6 test stub files from VALIDATION.md Wave 0 requirements exist in `tests/`. Vitest discovers and runs them without failures. Each file defines behavioral expectations that downstream plans will implement against.
</success_criteria>

<output>
After completion, create `.planning/phases/04-merge-workflow/04-00-SUMMARY.md`
</output>
